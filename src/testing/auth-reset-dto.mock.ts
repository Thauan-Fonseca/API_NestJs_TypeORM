import { AuthResetDTO } from '../auth/dto/auth-reset.dto';
import { resetToken } from './reset-token.mock';

export const AuthRestDTO: AuthResetDTO = {
  password: 'SD@#Ewdsd',
  token: resetToken,
};
